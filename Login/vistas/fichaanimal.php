<?php
	if (isset($_GET['id'])) {
		echo "hola soy un perrito ". $_GET['id'];
		// hace la consulta a la tabla perrito
		if () {// debe validar que el id que llega si es valido
			
		}else{
			//redirect a pagina principal
		}
	}else{
		echo "por favor ingrese un id valido";
		//redirect a pagina principal
	}
	//select * from adopcion where id= 
?>
 